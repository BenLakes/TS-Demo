// 函数的三种方式
// 声明式
function add(num: number, num2: number): number {
    return num + num2
}
// 表达式
let add1 = function(num1: number, num2: number):number {
    return num1 + num2;
}

// 箭头函数
let add2 = (num: number, num1: number): number => {
    return num + num1;
}

